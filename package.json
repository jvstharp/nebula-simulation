{
  "name": "nebula-simulation",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "next": "15.3.1"
  },
  "scripts": {
    "build": "cd nebula-app && npm install && npm run build",
    "dev": "cd nebula-app && npm run dev",
    "start": "cd nebula-app && npm run start"
  }
}
